{
  "name": "automation-test-case-2",
  "version": "1.0.0",
  "description": "Playwright automation suite for Demo App test case 2.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:demo": "DEMO_STEP_DELAY_MS=1000 PLAYWRIGHT_SLOW_MO=500 playwright test --headed --workers=1 --project=chromium",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "typescript": "^5.9.3"
  }
}
